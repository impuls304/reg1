# 🎫 Система регистрации на мероприятия

Полнофункциональная система регистрации участников на мероприятия с подтверждением по email и сохранением данных в PostgreSQL.

## ✨ Возможности

- ✅ Регистрация с проверкой email
- ✅ Двухэтапная верификация (код на email)
- ✅ Лимит участников
- ✅ Защита от ботов (honeypot, time-based)
- ✅ Rate limiting
- ✅ Хранение данных в PostgreSQL
- ✅ Красивый UI на React + Tailwind
- ✅ Соответствие требованиям РФ по хранению данных

## 📁 Структура проекта

\`\`\`
reg_mer/
├── app/              # Frontend (React + Vite)
│   ├── src/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── sections/
│   │   └── types/
│   ├── .env.example
│   └── package.json
│
├── server/           # Backend (Node.js + Express)
│   ├── src/
│   │   ├── db.ts
│   │   ├── email.ts
│   │   ├── index.ts
│   │   └── migrate.ts
│   ├── .env.example
│   └── package.json
│
├── DEPLOYMENT.md     # Подробная инструкция по развертыванию
├── QUICKSTART.md     # Быстрый старт
└── README.md         # Этот файл
\`\`\`

## 🚀 Быстрый старт

### Локальная разработка

#### 1. Установите PostgreSQL

\`\`\`bash
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
\`\`\`

#### 2. Создайте базу данных

\`\`\`bash
sudo -u postgres psql
CREATE DATABASE registration_db;
CREATE USER reg_user WITH PASSWORD 'dev_password';
GRANT ALL PRIVILEGES ON DATABASE registration_db TO reg_user;
\\q
\`\`\`

#### 3. Настройте Backend

\`\`\`bash
cd server
npm install
cp .env.example .env
# Отредактируйте .env (укажите данные БД и email)
npm run db:migrate
npm run dev
\`\`\`

Backend запустится на http://localhost:3000

#### 4. Настройте Frontend

\`\`\`bash
cd app
npm install
cp .env.example .env
# Отредактируйте .env (VITE_API_URL=http://localhost:3000)
npm run dev
\`\`\`

Frontend запустится на http://localhost:5173

## 📦 Развертывание на сервере

Смотрите подробную инструкцию в [DEPLOYMENT.md](DEPLOYMENT.md)

### Краткая версия:

1. Установите PostgreSQL
2. Настройте backend (установка зависимостей, .env, миграция БД)
3. Запустите backend через PM2
4. Соберите frontend
5. Настройте Nginx для раздачи файлов
6. Настройте SSL через certbot

## 🗄️ База данных

### Таблица \`registrations\`

| Поле              | Тип         | Описание                          |
|-------------------|-------------|-----------------------------------|
| id                | SERIAL      | Уникальный ID                     |
| first_name        | VARCHAR     | Имя                               |
| last_name         | VARCHAR     | Фамилия                           |
| email             | VARCHAR     | Email (уникальный)                |
| verification_code | VARCHAR(6)  | Код подтверждения                 |
| is_verified       | BOOLEAN     | Подтвержден ли email              |
| registered_at     | TIMESTAMP   | Дата регистрации                  |
| verified_at       | TIMESTAMP   | Дата подтверждения                |
| ip_address        | VARCHAR(45) | IP адрес                          |
| user_agent        | TEXT        | User Agent браузера               |

### Экспорт данных

\`\`\`bash
# Подключение к БД
sudo -u postgres psql -d registration_db

# Экспорт всех зарегистрированных в CSV
\\copy (SELECT first_name, last_name, email, verified_at FROM registrations WHERE is_verified = true ORDER BY verified_at) TO '/tmp/registrations.csv' CSV HEADER;
\`\`\`

## 📧 Email сервисы

Поддерживаются российские почтовые провайдеры:

- **Yandex** (smtp.yandex.ru:465)
- **Mail.ru** (smtp.mail.ru:465)
- **Rambler** (smtp.rambler.ru:465)

Необходимо создать "пароль приложения" в настройках вашей почты.

## 🔐 Безопасность

- ✅ Rate limiting (10 запросов за 15 минут с одного IP)
- ✅ Helmet.js для защиты заголовков
- ✅ Honeypot поле для защиты от ботов
- ✅ Проверка времени заполнения формы
- ✅ CORS настройка по доменам
- ✅ Валидация всех входных данных
- ✅ Логирование всех попыток регистрации

## 🇷🇺 Соответствие требованиям РФ

- ✅ База данных на вашем сервере
- ✅ Использование российских email сервисов
- ✅ Локальное хранение всех данных
- ✅ Нет передачи данных третьим лицам
- ✅ Возможность размещения на серверах в РФ

## 📊 API Endpoints

### GET /api/availability
Проверка доступности регистрации

**Ответ:**
\`\`\`json
{
  "available": true,
  "currentCount": 45,
  "maxParticipants": 100
}
\`\`\`

### POST /api/register
Регистрация (отправка кода на email)

**Запрос:**
\`\`\`json
{
  "firstName": "Иван",
  "lastName": "Иванов",
  "email": "ivan@example.com",
  "honeypot": "",
  "formStartTime": 1234567890
}
\`\`\`

**Ответ:**
\`\`\`json
{
  "success": true,
  "message": "Код подтверждения отправлен на ваш email"
}
\`\`\`

### POST /api/verify
Подтверждение кода

**Запрос:**
\`\`\`json
{
  "email": "ivan@example.com",
  "code": "123456"
}
\`\`\`

**Ответ:**
\`\`\`json
{
  "success": true,
  "message": "Регистрация успешно завершена!"
}
\`\`\`

### GET /api/stats
Статистика регистраций

**Ответ:**
\`\`\`json
{
  "total": 45,
  "pending": 3,
  "maxParticipants": 100,
  "recent": [...]
}
\`\`\`

## 🛠️ Технологии

### Frontend
- React 19
- TypeScript
- Vite
- Tailwind CSS
- Radix UI
- Lucide Icons

### Backend
- Node.js
- Express
- TypeScript
- PostgreSQL
- Nodemailer
- Helmet, CORS, Rate Limit

## 📝 Лицензия

MIT

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте [DEPLOYMENT.md](DEPLOYMENT.md) - там описаны типичные проблемы
2. Проверьте логи:
   - Backend: `pm2 logs registration-api`
   - Nginx: `sudo tail -f /var/log/nginx/error.log`
   - PostgreSQL: `sudo tail -f /var/log/postgresql/postgresql-*-main.log`

## 🎯 Следующие шаги

После развертывания:

1. Протестируйте регистрацию
2. Настройте резервное копирование БД
3. Настройте мониторинг (Grafana, PM2 Web)
4. Добавьте админ-панель для просмотра регистраций
5. Настройте автоматические уведомления

---

**Разработано с ❤️ для проведения мероприятий**
